otel_config_validator --help
>>>
NAME:
   otel_config_validator - Validate a configuration file against the OpenTelemetry Configuration Schema

USAGE:
   otel_config_validator [global options]

VERSION:
   0.1.0

GLOBAL OPTIONS:
   --output string, -o string  optionally where to output the configuration (as json or yaml) after variable expansion and validation
   --schema string, -s string  directory containing the json schema to use instead of the compiled in version
   --help, -h                  show help
   --version, -v               print the version
>>>= 0
