file_format: "1.1"

meter_provider:
  readers:
    - periodic:
        exporter:
          console:
  views:
    -
      # SNIPPET_START
      # Enable an opt-in metric instrument
      selector:
        instrument_name: my.opt_in.instrument
      stream:
        enabled/development: true
