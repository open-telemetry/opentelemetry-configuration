file_format: "1.1"
maturity_level: development

tracer_provider:
  processors:
    - simple:
        exporter:
          console:
  tracer_configurator:
    # SNIPPET_START
    default_config:
      enabled: false
    tracers:
      - name: io.opentelemetry.contrib.*
        config:
          enabled: true