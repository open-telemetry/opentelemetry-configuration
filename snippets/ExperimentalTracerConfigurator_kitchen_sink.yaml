file_format: "1.0"

tracer_provider:
  processors:
    - simple:
        exporter:
          console:
  tracer_configurator/development:
    # SNIPPET_START
    default_config:
      enabled: false
    tracers:
      - name: io.opentelemetry.contrib.*
        config:
          enabled: true