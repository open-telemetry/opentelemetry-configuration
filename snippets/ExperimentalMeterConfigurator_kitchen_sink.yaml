file_format: "1.1"
maturity_level: development

meter_provider:
  readers:
    - periodic:
        exporter:
          console:
  meter_configurator:
    # SNIPPET_START
    default_config:
      enabled: false
    meters:
      - name: io.opentelemetry.contrib.*
        config:
          enabled: true